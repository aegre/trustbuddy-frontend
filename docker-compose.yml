services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080}
    image: trustbuddy-frontend:local
    container_name: trustbuddy-frontend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
