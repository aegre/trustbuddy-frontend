import { SuccessScreen } from "@/features/wizard/screens/success-screen";

/** Thin route — success confirmation UI lives in features/wizard. */
export function SuccessRoute() {
  return <SuccessScreen />;
}
